Giphytool
Set a gif as your wallpaper. 

Requires
* GTK 3.0

Installation
./Autogen.sh && ./configure && make && make install

Usage
To set an gif in the center of the screen: giphytool -c some/path/animation.gif"
For full usage: giphytool --help


